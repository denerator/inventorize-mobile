/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { MainRouting } from './src/screens/main';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => MainRouting);
